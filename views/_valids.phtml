<?php if (isset($valids) && count($valids) > 0) : ?>

<div id="formMessage">  
    <div>
        <ul>
            <?php foreach ($valids as $valid) : ?>
            <li><?= $valid; ?></li>
            <?php endforeach; ?>
        </ul>
    </div>
</div>
<?php endif; ?>